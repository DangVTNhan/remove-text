t('missing_packages')


t('missing_packages')

t('code_is_invalid')

t('missing_packages')


t('Scan successfully {{value}} packages', {
    value: dataScanned.find((item) => item.so === value)
      ?.scannedQuantity
  })